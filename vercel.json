{
  "buildCommand": "cd pto-connect && pnpm run build",
  "outputDirectory": "pto-connect/dist",
  "installCommand": "cd pto-connect && pnpm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
